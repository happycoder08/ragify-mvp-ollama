[pytest]
pythonpath = .
asyncio_mode = auto
testpaths = tests
addopts = -m "not integration"
markers =
    ci_fast: Fast CI tests using mock LLM and TF-IDF embeddings
    integration: Integration tests using real Ollama embeddings (skipped by default)